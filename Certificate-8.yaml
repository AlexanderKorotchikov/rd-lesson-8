apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: backend-8-cert
spec:
  secretName: backend-8-cert
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  commonName: app8.loc
  dnsNames:
    - app8.loc